name: Label PRs

on:
  pull_request:
    types: [ opened ]

jobs:
  pr-labeler:
    runs-on: ubuntu-latest
    steps:
      - uses: ffittschen/pr-branch-labeler@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}